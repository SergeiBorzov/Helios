#include "material.h"
#include "bxdf.h"

namespace Helios {
    /*void Matte::ProduceBSDF(RayHitRecord& record) const {
        // record->bsdf =  make_shared<BSDF>(record);
        // record->bsdf->Add(new Lambertian(diffuse));
    }*/
}